/*
Multiply each element in sourceArray with the corresponding value in offsetArray. Store each value in a new array named finalArray.
*/
let sourceArray = [ 40, 50, 60, 70 ]; // Tests will use different arrays
let offsetArray = [ 2, 5, 8, 4 ]; // Tests will use different arrays
let finalArray = [];

/* Your solution goes here */
for (let i = 0; i < finalArray.length; i++) {
      finalArray = sourceArray[i] * offsetArray[i];
   }

console.log(finalArray);
